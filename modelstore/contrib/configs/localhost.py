# Which models are we using?
MODELS = ['arome', 'proff']

# Define ALL the nodes in this group, including the one configured
SIBLINGS = ['127.0.0.1', '157.249.113.93', '192.168.1.1']

# Which WDB server is connected to this node?
WDB_HOST = 'localhost'

# Which port is the WDB ZMQ daemon running on?
WDB_PORT = 5556

# Which port in the Coordinator cluster is publishing messages?
PUB_PORT = 5557

# Which interface to bind to
PUB_HOST = '127.0.0.1'
